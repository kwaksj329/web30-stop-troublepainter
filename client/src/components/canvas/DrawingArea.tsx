import CanvasToolBar from './CanvasToolbar';
import MainCanvas from './MainCanvas';

const DrawingArea = () => {
  return (
    <section>
      <MainCanvas />
      <CanvasToolBar />
    </section>
  );
};

export default DrawingArea;
