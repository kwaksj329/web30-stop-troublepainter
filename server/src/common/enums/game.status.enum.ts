export enum PlayerStatus {
  NOT_READY = 'NOT_READY',
  READY = 'READY',
  PLAYING = 'PLAYING',
}

export enum PlayerRole {
  PAINTER = 'PAINTER',
  DEVIL = 'DEVIL',
  GUESSER = 'GUESSER',
}

export enum RoomStatus {
  WAITING = 'WAITING',
  IN_GAME = 'IN_GAME',
}
